export enum PayeePaymentStatus {
  COMPLETED = 'completed',
  DUE_NOW = 'due_now',
  OVERDUE = 'overdue',
  PENDING = 'pending',
}

export enum AppEvent {
  SHOW_PROGRESS_BAR = 'showProgresBar',
  HIDE_PROGRESS_BAR = 'hideProgresBar',
}
